mixin lower_fv(obj)
  .lower-fv(style=`background: linear-gradient(to bottom, rgba(0, 0, 0, 0.3) 0%, rgba(0, 0, 0, 0.3) 100%), url(${obj.img}) no-repeat center center / cover;`)
    h1.lower-fv-title !{pageTitle}
    // /.l-container
